-- Retrieve the names of all songs from the 'songs' table, ordered by their tempo
SELECT name  -- Selects the 'name' column from the 'songs' table
FROM songs  -- Specifies the 'songs' table as the source of data
ORDER BY tempo;  -- Orders the results by the 'tempo' column in ascending order
